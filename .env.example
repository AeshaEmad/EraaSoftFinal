# Use deployment environment variables or `dotnet user-secrets`; never commit real values.
ConnectionStrings__DefaultConnection=
EmailSettings__SenderEmail=
EmailSettings__SenderPassword=
Stripe__PublishableKey=
Stripe__SecretKey=
Stripe__WebhookSecret=

# One-time bootstrap only. Remove both after the first login and password change.
Security__BootstrapAdminEmail=
Security__BootstrapAdminPassword=
